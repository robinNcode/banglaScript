functions {
  FunctionDeclaration = "ফাংশন" identifier "(" Params? ")" "{" Program "}"
  Params = identifier ("," identifier)*

  FunctionCall = identifier "(" Arguments? ")" ";"
  Arguments = Expression ("," Expression)*
}
